---
interface Props {
	label: string
	title: string
	url: string
	class?: string
}
const { label, title, url } = Astro.props
---

<p class:list={['text-gray', Astro.props.class]}>
	{label}: <a class='text-white' href={url} target='_blank'>{title}</a>
</p>
