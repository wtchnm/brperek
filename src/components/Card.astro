---
interface Props {
	title: string
	subtitle: string
	class?: string
}
const { title, subtitle } = Astro.props
---

<div
	class:list={[
		'bg-primary p-6 text-center shadow-2xl md:p-10',
		Astro.props.class
	]}
>
	<p class='text-xl font-medium md:text-5xl'>{title}</p>
	<p class='text-sm md:text-2xl'>{subtitle}</p>
</div>
