---
export interface Props {
	title: string
	subtitle: string
}
const { title, subtitle } = Astro.props
---

<article class='px-4 py-6 text-center shadow-xl bg-background'>
	<h3 class='text-xl font-medium'>{title}</h3>
	<p>{subtitle}</p>
</article>
